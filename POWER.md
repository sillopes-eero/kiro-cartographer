---
name: cartographer
description: "Maps and documents codebases of any size by orchestrating parallel subagents"
author: "Bootoshi, sillopes-eero"
version: "2.0.0"
triggers:
  - "map this codebase"
  - "cartographer"
  - "create codebase map"
  - "document the architecture"
  - "understand this codebase"
---

# Cartographer

Maps codebases of any size using parallel subagents. Creates `docs/CODEBASE_MAP.md` with architecture overview, file purposes, dependencies, and navigation guides. Automatically detects if a map exists and updates only changed sections.

## Workflow Overview

Cartographer uses a phased approach where the orchestrating agent plans the work and delegates file reading to subagents via `invokeSubAgent`. Even for small codebases, file reading is always delegated to subagents — the orchestrator never reads codebase files directly.

### Phases

1. **Check** — Determine if `docs/CODEBASE_MAP.md` already exists. If it does, detect changes using git commit hash, git timestamp, or scanner diff as fallbacks. Decide between full mapping and update mode.
   → See [steering/check-phase.md](steering/check-phase.md)

2. **Scan** — Run the scanner script (bundled with this Power) against the user's project:

   ```bash
   uv run <POWER_ROOT>/scripts/scan-codebase.py . --format json
   ```

   Fallbacks: `python3 <POWER_ROOT>/scripts/scan-codebase.py . --format json` or `python <POWER_ROOT>/scripts/scan-codebase.py . --format json`

3. **Plan** — Analyze scan output and group files into subagent assignments. Balance token budgets at ~80,000 tokens per subagent (conservative to prevent context overflow). If more than 5 top-level modules are detected, prompt the user about Split_Mode.
   → See [steering/scan-plan-phase.md](steering/scan-plan-phase.md)

4. **Analyze (Pass 1)** — Spawn subagents in parallel. Each reads its assigned files and writes a concise raw summary to `docs/.cartographer/reports/<id>.md`. Reports are persisted to disk so progress survives subagent crashes. Failed subagents are retried automatically.

5. **Analyze (Pass 2)** — Read the raw summaries from disk and enrich them with cross-file relationships, architectural insights, and detailed module narratives. Enriched reports overwrite the raw ones on disk.
   → See [steering/analyze-phase.md](steering/analyze-phase.md)

6. **Synthesize** — Read enriched reports from `docs/.cartographer/reports/`, merge them, deduplicate overlapping analysis, identify cross-cutting concerns, and build Mermaid architecture diagrams.

7. **Write** — Create or update `docs/CODEBASE_MAP.md` with full frontmatter and all required sections. In Split_Mode, write per-module files under `docs/codebase_map_modules/`. In update mode, only refresh affected module sections.
   → See [steering/synthesize-write-phase.md](steering/synthesize-write-phase.md)

## Scanner Script

The scanner is a Python script at `scripts/scan-codebase.py` **within this Power's installation directory**. It recursively scans a directory tree, respects `.gitignore` patterns, and counts tokens per file using tiktoken. It supports JSON, tree, and compact output formats.

### Locating the Script

The script is bundled with this Power, not with the user's project. To find it at runtime, resolve the path relative to this Power's root directory. In all invocations below, `<POWER_ROOT>` refers to the directory containing this POWER.md file.

### Invocation

Preferred (auto-installs tiktoken via UV inline dependencies):

```bash
uv run <POWER_ROOT>/scripts/scan-codebase.py . --format json
```

Fallback if UV is not available:

```bash
python3 <POWER_ROOT>/scripts/scan-codebase.py . --format json
```

If tiktoken is missing and not using UV:

```bash
pip install tiktoken
```

The `.` argument tells the scanner to scan the **user's current working directory** (the project being mapped), while the script path points to the Power's installation directory.

## Output

The primary output is `docs/CODEBASE_MAP.md` containing:

- System overview with Mermaid architecture diagram
- Annotated directory structure
- Module guide with per-module details
- Data flow diagrams
- Conventions and gotchas
- Navigation guide for common tasks

When Split_Mode is active, the index file contains module summaries with links to detailed per-module files under `docs/codebase_map_modules/`.

> Auto-generated by Cartographer (Kiro Power)
